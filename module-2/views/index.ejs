<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitHub User Info</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            text-align: center; 
            margin: 50px; 
            background-image: url(./background.gif); 
            color: yellow; 
        }
        form { margin-bottom: 20px; }
        ul { list-style: none; padding: 0; }
        li { background: #f4f4f4; margin: 10px auto; padding: 10px; width: 50%; border-radius: 5px; color: blueviolet; }
        button {display: inline-block;
                padding: 15px 25px;
                font-size: 10px;
                cursor: pointer;
                text-align: center;
                text-decoration: none;
                outline: none;
                color: #fff;
                background-color: #04AA6D;
                border: none;
                border-radius: 15px;
                box-shadow: 0 9px #999;
            }
            .button:active {
                background-color: #be25d6;
                box-shadow: 0 5px #ff0e0e;
                transform: translateY(4px);
}
        button:hover {box-shadow: 0 12px 16px 0 rgba(249, 248, 248, 0.24), 0 17px 50px 0 rgba(7, 234, 255, 0.19);}
    </style>
</head>
<body >
    <h2>GitHub User Lookup</h2>
    <form action="/fetch-github" method="POST">
        <input type="text" name="username" placeholder="Enter GitHub Username" required style="padding: 8px;">
        <button type="submit">Search</button>
    </form>

    <% if (error) { %>
        <p style="color: red; body {background-color: antiquewhite;}"><%= error %></p>
    <% } %>

    <% if (userData) { %>
        <h3>Public Info for <%= userData.login %></h3>
        <ul>
            <li><strong>Username:</strong> <%= userData.login %></li>
            <li><strong>Name:</strong> <%= userData.name || 'N/A' %></li>
            <li><strong>Bio:</strong> <%= userData.bio || 'N/A' %></li>
            <li><strong>Location:</strong> <%= userData.location || 'N/A' %></li>
            <li><strong>Public Repos:</strong> <%= userData.public_repos %></li>
            <li><strong>Followers:</strong> <%= userData.followers %></li>
            <li><strong>Following:</strong> <%= userData.following %></li>
            <li><strong>GitHub Profile:</strong> <a href="<%= userData.html_url %>" target="_blank">View Profile</a></li>
            <li><img src="<%= userData.avatar_url %>" alt="Avatar" width="100"></li>
        </ul>
    <% } %>
</body>
<script src="./main.js"></script>
</html>
